"""Future package boundaries for BTC AI Terminal.

The production entry points still use the legacy root modules. Subpackages are
introduced incrementally and must not change those imports until compatibility
tests cover each migration.
"""
